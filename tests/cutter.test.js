import { cutter } from '../components/util/algorithms'
describe('零轴计算 => cutter => 复杂算法',()=>{
    let test1 = {
        max : 100,
        min : -20,
        len : 5,
        height : 1000
    }

    it(`----- test 1 ---`, () => {
        expect(cutter(test1)).toBe({
            item_H,
            cut,
            zero_top,
            zero_bottom,
            all_line,
            n_max,
            n_min
        });
    });

})